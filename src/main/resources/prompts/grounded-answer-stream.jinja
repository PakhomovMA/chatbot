{% set sources = "the numbered evidence passages you are given" %}
{% include "_grounding_rules" %}
- Cite the passages you use inline as [n] (for example "restart with systemctl [2]"). Use only numbers that appear in the evidence list.
- If the evidence does not contain what is needed, say so plainly in one sentence and do not invent an answer. In that case end your reply with a final line exactly like: `INSUFFICIENT: <what is missing>`.
- Use Markdown. Output only the answer text, no preamble, no JSON.
